server:
  port: 8070
  
spring:
  application:
    name: sidecar
    
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8761/eureka 
      
sidecar:
  port: 8060
  health-uri: http://localhost:8060/health.json